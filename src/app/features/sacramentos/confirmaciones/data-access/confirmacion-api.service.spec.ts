import { provideHttpClient } from '@angular/common/http';
import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing';
import { TestBed } from '@angular/core/testing';
import { RuntimeConfigService } from '../../../../core/config/runtime-config.service';
import { ConfirmacionApiService } from './confirmacion-api.service';
describe('ConfirmacionApiService',()=>{let service:ConfirmacionApiService;let http:HttpTestingController;const apiBaseUrl='https://localhost:9001/api';beforeEach(()=>{TestBed.configureTestingModule({providers:[ConfirmacionApiService,provideHttpClient(),provideHttpClientTesting(),{provide:RuntimeConfigService,useValue:{config:{apiBaseUrl}}}]});service=TestBed.inject(ConfirmacionApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('should request filtered confirmations',()=>{service.getList({texto:'Perez',idLibroSacramental:8}).subscribe();const req=http.expectOne(r=>r.url===`${apiBaseUrl}/Confirmacion`);expect(req.request.params.get('texto')).toBe('Perez');expect(req.request.params.get('idLibroSacramental')).toBe('8');req.flush([]);});it('should validate confirmation sequence',()=>{service.validatePartida({idLibroSacramental:8,idFolioSacramental:3,numeroPartida:'10'}).subscribe();const req=http.expectOne(`${apiBaseUrl}/Confirmacion/validar-partida`);expect(req.request.method).toBe('POST');req.flush({});});it('should request next part by book',()=>{service.getSiguientePartida(8).subscribe();const req=http.expectOne(`${apiBaseUrl}/Confirmacion/libro/8/siguiente-partida`);expect(req.request.method).toBe('GET');req.flush({});});});
