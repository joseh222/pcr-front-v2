import { provideHttpClient } from '@angular/common/http';
import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing';
import { TestBed } from '@angular/core/testing';
import { RuntimeConfigService } from '../../../../core/config/runtime-config.service';
import { MatrimonioApiService } from './matrimonio-api.service';
describe('MatrimonioApiService',()=>{let service:MatrimonioApiService;let http:HttpTestingController;const apiBaseUrl='https://localhost:9001/api';beforeEach(()=>{TestBed.configureTestingModule({providers:[MatrimonioApiService,provideHttpClient(),provideHttpClientTesting(),{provide:RuntimeConfigService,useValue:{config:{apiBaseUrl}}}]});service=TestBed.inject(MatrimonioApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('should request filtered marriages',()=>{service.getList({texto:'Perez',dni:'12345678'}).subscribe();const req=http.expectOne(r=>r.url===`${apiBaseUrl}/Matrimonio`);expect(req.request.params.get('texto')).toBe('Perez');expect(req.request.params.get('dni')).toBe('12345678');req.flush([]);});it('should validate marriage sequence',()=>{service.validatePartida({idLibroSacramental:9,idFolioSacramental:4,numeroPartida:'20'}).subscribe();const req=http.expectOne(`${apiBaseUrl}/Matrimonio/validar-partida`);expect(req.request.method).toBe('POST');req.flush({});});it('should request next part by book',()=>{service.getSiguientePartida(9).subscribe();const req=http.expectOne(`${apiBaseUrl}/Matrimonio/libro/9/siguiente-partida`);expect(req.request.method).toBe('GET');req.flush({});});});
